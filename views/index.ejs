<!DOCTYPE html>
<html lang="en">
<head>
    <%-include("./partials/head")%> 
</head>
<body>
    <h1>BUDGTR - Index</h1>
    <table>
        <tr>
            <th>DATE</th>
            <th>NAME</th>
            <th>FROM</th>
            <th>AMOUNT</th>
            <th>TAGS</th>
        </tr>
        <% for (let i = 0; i < budget.length; i++) { %>
        <tr>
            <td><%= budget[i].date %></td>
            <td><a href=""><%= budget[i].name %> </a></td>
            <td><%= budget[i].from %> </td>
            <td><%= budget[i].amount %> </td>
            <td><%= budget[i].tags %> </td> 
        </tr>
    <% } %> 
    </table>
</body>
</html>